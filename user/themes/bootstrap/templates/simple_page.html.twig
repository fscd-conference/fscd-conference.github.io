{% extends 'partials/base.html.twig' %}

{% block content %}
    <div class="container">
		<div class="row">
			<div class="col-lg-12 text-center">
				<h2>{{ page.title }}</h2>
				<hr class="star">
			</div>
		</div>
	{{ page.content }}
    </div>
{% endblock %}
